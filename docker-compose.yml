services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: baseballai
      POSTGRES_USER: baseballai
      POSTGRES_PASSWORD: baseballai_dev
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U baseballai"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
